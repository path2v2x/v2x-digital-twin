# Boot proof at 2026-08-25T13:47:38-07:00 — twin-server f4bdad8+

$ pgrep -af "CarlaUE4|CarlaUnreal|CarlaServer|scenario_runner"   # CARLA binaries
(none — zero CARLA processes)

$ pgrep -af python | grep -i carla    # no python CARLA bridge
(none)

Note: a legacy scripts/wait-for-carla.sh poll loop may appear under pgrep -f carla; it is a shell script waiting for a CARLA that never runs (legacy scripts/ untouched this wave), not a CARLA process.

$ ss -ltnp | grep -E "8765|8090"     # twin-server owns both ports
LISTEN 0      511                              *:8090             *:*    users:(("node",pid=3105867,fd=38))      
LISTEN 0      511                              *:8765             *:*    users:(("node",pid=3105867,fd=32))      
